name: LocalSpace Release STOPPED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  stopped:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "LocalSpace/event platform release is intentionally stopped."
