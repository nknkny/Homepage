name: Retired - Akiya legacy hosting migration prepare

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RETIRED - canonical hosting migration is complete; this legacy workflow must not write directly to main.'
