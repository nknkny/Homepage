name: LocalSpace Cloudflare STOPPED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  stopped:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "LocalSpace/event platform deployment is intentionally stopped."
