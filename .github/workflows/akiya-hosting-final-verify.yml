name: Retired - Akiya legacy hosting final verify

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RETIRED - obsolete project hostname and admin markers. Canonical verification is Cloudflare stage1 v3.'
