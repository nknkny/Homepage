name: Event Platform Quality STOPPED

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  stopped:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Event platform is intentionally stopped."
