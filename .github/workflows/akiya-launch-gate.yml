name: Retired - Akiya legacy launch gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RETIRED - superseded by current exhaustive audit, live production check, admin live check, and Cloudflare stage1 v3 verification.'
