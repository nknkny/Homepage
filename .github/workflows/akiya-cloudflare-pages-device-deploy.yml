name: Retired - Akiya legacy Cloudflare Pages device deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RETIRED - canonical production deploy is Akiya Cloudflare stage1 v3 to akiya-pocket-aomori.pages.dev.'
