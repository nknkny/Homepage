name: Retired - Akiya legacy full GitHub Pages deploy

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RETIRED - customer production is Cloudflare Pages; GitHub Pages deploy-public-pages maintains redirects and the admin control center.'
