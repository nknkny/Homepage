name: Retired - Akiya registration diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RETIRED - legacy diagnostic created synthetic member state. Current production checks are non-mutating.'
