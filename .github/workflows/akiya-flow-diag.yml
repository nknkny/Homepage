name: Retired - Akiya request/payment diagnostic

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RETIRED - legacy diagnostic created synthetic member/request state. Current production verification is non-mutating.'
