name: Retired - Akiya legacy read-only audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RETIRED - this audit targeted the obsolete rapid-hat Worker. Canonical production is akiya-pocket-aomori.pages.dev.'
