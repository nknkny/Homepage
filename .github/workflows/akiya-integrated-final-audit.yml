name: Retired - Akiya legacy integrated final audit

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'RETIRED - this audit depended on the retired unified API. Use the current exhaustive/live/admin/Cloudflare verification gates.'
