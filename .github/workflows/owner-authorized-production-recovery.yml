name: Owner Authorized Production Recovery

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-after-cloudflare-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Recovery workflow disabled
        run: echo "Cloudflare owner-authorized deployment completed; automatic PR trigger disabled."
